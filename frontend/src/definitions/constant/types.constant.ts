export enum RoleType {
  Admin,
  Employee
}

export enum TagType {
  Normal,
  Special,
  New
}

export enum OrderStatus {
  Pending,
  Completed,
  Cancelled
}

export enum PermissionType {
  Dashboard,
  Menu,
  Staff,
  Inventory,
  Order,
  Settings
}

export enum TableStatus {
  Empty,
  Busy
}
