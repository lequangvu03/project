export enum RoleType {
  Admin,
  Employee
}

export enum TagType {
  Normal,
  Special,
  New
}

export enum OrderStatus {
  Pending,
  Completed,
  Cancelled
}

export enum PermissionType {
  Dashboard,
  Reports,
  Inventory,
  Orders,
  Customers,
  Settings
}

export enum TableStatus {
  Empty,
  Busy
}